<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Mode Palette (Overrides from values/colors.xml using dm_ prefixed colors) -->
    <color name="primary">@color/dm_primary</color>
    <color name="secondary">@color/dm_secondary</color>
    <color name="background">@color/dm_background</color>
    <color name="text_primary">@color/dm_text_primary</color>
    <color name="accent">@color/dm_accent</color>

    <!-- Overrides for other common colors for Dark Mode -->
    <color name="primary_variant">@color/dm_primary_variant</color>
    <color name="primary_light">@color/dm_primary_light</color>
    <color name="secondary_variant">@color/dm_secondary_variant</color>
    <color name="secondary_light">@color/dm_secondary_light</color>
    <color name="surface">@color/dm_surface</color>
    <color name="text_secondary">@color/dm_text_secondary</color>
    <color name="text_hint">@color/dm_text_hint</color>

    <color name="error">@color/dm_error</color>
    <color name="error_light">@color/dm_error_light</color>
    <color name="success">@color/dm_success</color>

    <!-- Social Login Colors - if they need specific dark mode versions, define them here -->
    <!-- Example: <color name="google_blue_dark_theme">#...</color> -->
    <!-- For now, they will fall back to the definitions in values/colors.xml -->
</resources>